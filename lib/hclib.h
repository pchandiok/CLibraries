#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef char* string;

char* GetString(const char *);
double GetDouble(const char *);
float GetFloat(const char *);
int GetInt(const char *);
